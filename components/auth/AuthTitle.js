const DialogTitle = ({ title, subtitle }) => (
  <div className="mb-4 mt-8">
    <h1 className="text-[24px] font-bold ">{title}</h1>
    <p className="text-[16px] text-gray-700">{subtitle}</p>
  </div>
);
export default DialogTitle;
